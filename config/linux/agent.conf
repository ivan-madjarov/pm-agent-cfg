# Linux-specific PM+ Agent Configuration

[paths]
log_directory = /var/log/pm-agent
data_directory = /var/lib/pm-agent  
config_directory = /etc/pm-agent

[service]
user = pm-agent
group = pm-agent
pid_file = /var/run/pm-agent.pid

[system]
use_systemd = true
syslog_facility = daemon

